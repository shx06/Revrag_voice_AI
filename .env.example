# ─── LiveKit credentials ───────────────────────────────────────────────────
# URL of your LiveKit server (Cloud or self-hosted).
LIVEKIT_URL=wss://your-project.livekit.cloud

# API key and secret from your LiveKit project dashboard.
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# ─── OpenAI credentials ────────────────────────────────────────────────────
# Used for:
#   • STT — OpenAI Whisper transcription API
#   • TTS — OpenAI text-to-speech API
# A single key covers both services.  The free-tier / pay-as-you-go tier is
# sufficient for development.
OPENAI_API_KEY=your_openai_api_key
