# ── LiveKit connection ────────────────────────────────────────────────────────
# WebSocket URL of your LiveKit Cloud project or self-hosted server.
LIVEKIT_URL=wss://your-project.livekit.cloud

# API credentials — generate in the LiveKit Cloud dashboard or with livekit-cli.
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# ── OpenAI (STT + TTS) ───────────────────────────────────────────────────────
# Used by livekit-plugins-openai for Whisper (speech-to-text) and TTS-1
# (text-to-speech).  No LLM API calls are made by this agent.
OPENAI_API_KEY=your_openai_api_key
